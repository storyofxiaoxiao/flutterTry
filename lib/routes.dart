
import 'package:flutter_app/screens/HomePage.dart';
import 'package:flutter_app/screens/MyAccountPage.dart';

var staticRoutes = {
  "/sign-in": (context) => HomePage(), // 登录
  "/sign-up": (context) => MyAccountPage(), // 注册
};
